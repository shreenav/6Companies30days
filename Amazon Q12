string colName (long long int n)
    {
        string ans = "";
        while(n>0){
            char cur = 'A' + (n-1)%26;
            ans = cur + ans;
            n = (n-1)/26;
        }
        return ans;
    }
